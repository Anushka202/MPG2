var database;
var playerCount, form, player, game;
var gameState = 0;
var canvas, bgImg


function setup(){
 camvas = createCanvas(400, 400);


  database = firebase.database();
  console.log(database);
  
}

function draw(){
  
  
}

